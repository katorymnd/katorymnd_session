<?php

/**
 * call the page that has our session class here.
 * 
 * You must include  'katorymnd_session.php' page on every page you want to use the session
 * 
 */
include'./katorymnd_session.php';

/**
 * the session is already started
 * all you need is to set the session and amy be 
 */

 //$katorymnd_session->set_session('name', 'John'); // set session

//print $katorymnd_session->get_session('name');

//$katorymnd_session->destroy_session(); // delete  session



//$katorymnd_session->display_session(); // display session
